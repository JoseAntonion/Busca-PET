package com.example.buscapet.data.database.dao

import androidx.room.Dao

@Dao
interface CatBreedDao {
    //@Query("SELECT * FROM breed")
    //fun getAll(): LiveData<List<CatBreedEntity>>
//
    //@Insert(onConflict = OnConflictStrategy.REPLACE)
    //suspend fun insertAll(list: List<CatBreedEntity>)
//
    //@Insert(onConflict = OnConflictStrategy.REPLACE)
    //suspend fun insert(catBreed: CatBreedEntity)
//
    //@Query("DELETE FROM breed")
    //suspend fun deleteAll()
}